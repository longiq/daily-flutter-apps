{
  "name": "ai-proxy",
  "version": "1.0.0",
  "description": "Backend proxy dùng chung cho các app Flutter AI của longiq — giấu API key Gemini, expose 1 endpoint /api/generate, deploy free trên Render.",
  "private": true,
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "express-rate-limit": "^7.4.0",
    "dotenv": "^16.4.5"
  }
}
